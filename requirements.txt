# .docx 표·셀·문단 단위 주입을 위해 사용. 표준 라이브러리에는 .docx 편집 기능이 없어 도입.
# 대안: docxtpl(Jinja 템플릿 방식)도 있으나, 기준 템플릿이 플레이스홀더 없는 완성 문서라
#       기존 서식을 그대로 둔 채 값만 바꾸는 본 방식에는 python-docx가 더 직접적.
python-docx>=1.1.0

# Gemini API 호출용 공식 SDK. 구 google-generativeai는 신규 SDK로 대체되는 추세라
# 신규 통합 SDK(google-genai)를 사용. 대안: REST 엔드포인트를 requests로 직접 호출.
google-genai>=1.0.0

# .env 파일에서 환경변수 로드. 대안: 셸에서 직접 export하면 이 의존성은 불필요.
python-dotenv>=1.0.0

# PDF/HWP 변환용. 설치된 MS Word/한컴오피스를 COM으로 자동화해 .docx 서식을 보존한 채
# 변환한다(재렌더링 금지). Windows 전용이라 환경 마커로 한정. 대안: LibreOffice soffice 서브프로세스.
pywin32>=306; sys_platform == "win32"
